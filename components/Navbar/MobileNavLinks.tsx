import styles from "./MobileNavLinks.module.scss";
import { ReactElement } from "react";

const MobileNavLinks = (): ReactElement | null => {
  return <div className={styles.mobileNavLinks}></div>;
};

export default MobileNavLinks;
